<h1>Descontos</h1>

<table>
  <tr>
  </tr>

<% @descontos.each do |desconto| %>
  <tr>
    <td><%= link_to 'Show', desconto %></td>
    <td><%= link_to 'Edit', edit_desconto_path(desconto) %></td>
    <td><%= link_to 'Destroy', desconto, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Conceder desconto', new_admin_desconto_path %>